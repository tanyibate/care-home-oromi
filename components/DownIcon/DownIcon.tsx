import { FunctionComponent } from "react";
import styles from "./DownIcon.module.scss";

const DownIcon: FunctionComponent = () => {
  return <img className={styles.downIcon} alt="" src="Down.svg" />;
};

export default DownIcon;
